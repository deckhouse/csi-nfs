# Edition module settings, default ee
{{- $_ := set . "MODULE_EDITION" (env "MODULE_EDITION" "ee") }}

# Component versions are in oss.yaml; images use get_oss_version_by_id (see .werf/defines/oss-yaml.tmpl).
